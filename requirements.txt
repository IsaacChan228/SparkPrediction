# Runtime requirements for this project.
# This project is designed for Python 3.11 and Java JDK 17.
# The code uses standard library modules (csv, re, pathlib, concurrent.futures, typing, etc.)

# System requirements (install separately; not available via pip)
# PySpark requires a Java runtime (JDK). Install Java Development Kit (JDK) 17 (64-bit).
# Examples:
#  - Windows (Chocolatey):  choco install temurin17jdk -y
#  - macOS (Homebrew):      brew install --cask temurin
#  - Ubuntu/Debian:         sudo apt install openjdk-17-jdk
# After installing, set JAVA_HOME to the JDK path and add $JAVA_HOME/bin to PATH.
#
# To install Python dependencies:
#   pip install -r requirements.txt

# PySpark is required to run the Spark-based training pipeline
pyspark>=3.1.2

# Optional helper to locate Spark when running locally
findspark>=1.4.2

# pandas and numpy used by spark_training.py
pandas>=1.2.0
numpy>=1.19.0

# PyTorch: for GPU-enabled wheels, pip needs the PyTorch wheel index.
# Uncomment and adjust the CUDA tag if you want to install a CUDA build.
# Example (CUDA 13.2):
# --extra-index-url https://download.pytorch.org/whl/cu132
# Then install the packages below (torch, torchvision, torchaudio).
# For CPU-only install, pip will install CPU wheels from the default index.
torch
torchvision
torchaudio

# sentence-transformers is used for the BERT encoding.
sentence-transformers>=2.2.2

psutil