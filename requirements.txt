# Runtime requirements for this project.
# The code uses only Python standard library modules (csv, re, pathlib,
# concurrent.futures, typing, etc.) so no mandatory external packages are
# required for Python 3.8+.
#
# Optional backports for older Python versions:
dataclasses; python_version < "3.7"
typing-extensions; python_version < "3.8"

# To install (if any):
#   pip install -r requirement.txt

# PySpark is required to run the Spark-based training pipeline
pyspark>=3.1.2

# Optional helper to locate Spark when running locally
findspark>=1.4.2

# PyTorch, pandas and numpy used by spark_training.py
torch>=1.12.0
pandas>=1.2.0
numpy>=1.19.0

# CUDA-enabled PyTorch (example for CUDA 13.2). When installing from this
# requirements file, pip will use the PyTorch CUDA wheel index to fetch
# GPU-enabled wheels. If your system has a different CUDA runtime, change
# the URL (e.g. cu118, cu121, cu132) to a compatible one.
--extra-index-url https://download.pytorch.org/whl/cu132
torch
torchvision
torchaudio
